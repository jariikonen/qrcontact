{
  "name": "qrcontact-monorepo",
  "version": "0.0.0",
  "private": true,
  "description": "A monorepo for QRContact",
  "author": "Jari Ikonen",
  "workspaces": [
    "packages/frontend",
    "packages/backend"
  ],
  "scripts": {
    "cleanup": "rm -rf node_nodules && npm run cleanup -w qrcontact-frontend"
  }
}
