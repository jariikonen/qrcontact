{
  "name": "qrcontact-monorepo",
  "version": "0.0.0",
  "private": true,
  "description": "A monorepo for QRContact",
  "author": "Jari Ikonen",
  "license": "MIT",
  "workspaces": [
    "packages/frontend",
    "packages/backend"
  ],
  "scripts": {
    "cleanup": "rm -rf ./node_modules && npm run cleanup -w frontend"
  }
}
